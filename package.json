{
  "name": "trego-livraison-rapide",
  "version": "1.0.0",
  "description": "LIVRAISON RAPIDE - Rapid Delivery Management System",
  "scripts": {
    "build": "cd Frontend && npm install && npm run build",
    "dev": "cd Frontend && npm start",
    "install": "cd Frontend && npm install"
  },
  "engines": {
    "node": "18.x"
  }
}
